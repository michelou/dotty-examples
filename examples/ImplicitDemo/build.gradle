plugins {
    id "java"
}

apply from: "../common.gradle"

group "$appGroup"
version = "$appVersion"

description """Gradle example project that compiles Scala 3 code"""

run.doFirst {
    main scalaMainClassName
    args ""
}
