plugins {
    id "java"
}

group "$appGroup"
version "$appVersion"

description """Gradle example project to build/run Scala 3 applications"""

apply from: "../common.gradle"

run.doFirst {
    args "1"
}
